# Daily Progress: 2026-06-08

Day: Monday

## Tasks Completed

- Built PySpark preprocessing workflow for the real Bank Marketing dataset.
- Generated `user_id` values and encoded `y` into `label`.
- Excluded `duration` from the model feature set due to leakage risk.
- Built Spark ML preprocessing stages with `StringIndexer`, `OneHotEncoder`, and `VectorAssembler`.
- Trained and evaluated Spark MLlib Logistic Regression.
- Generated Logistic Regression prediction output with the agreed serving schema.
- Built FastAPI lookup MVP for `GET /prediction/{user_id}`.
- Added not-found handling with request metadata.
- Added `/health` endpoint with record count and cache counters.
- Added basic API tests.
- Updated API docs and API scope.

## Files Created or Updated

- `pyspark_project/scripts/pipeline_utils.py`
- `pyspark_project/scripts/preprocess_pyspark.py`
- `pyspark_project/scripts/train_logistic_regression.py`
- `pyspark_project/reports/PYSPARK_PREPROCESSING_REPORT.md`
- `pyspark_project/reports/LOGISTIC_REGRESSION_REPORT.md`
- `pyspark_project/data/processed/.gitkeep`
- `api_project/app/main.py`
- `api_project/app/repository.py`
- `api_project/app/cache.py`
- `api_project/app/schemas.py`
- `api_project/tests/test_prediction_api.py`
- `docs/api/API_DOCS.md`
- `docs/api/API_SCOPE.md`
- `docs/daily/2026-06-08.md`
- `.gitignore`

Generated but not committed:

- `pyspark_project/data/processed/predictions_lr.parquet`

## Commands Run

Dependency setup:

```powershell
& 'C:\Users\nmp10\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m pip install fastapi uvicorn httpx
& 'C:\Users\nmp10\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m pip install pyspark
```

Static compile:

```powershell
& 'C:\Users\nmp10\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m py_compile pyspark_project\scripts\pipeline_utils.py pyspark_project\scripts\preprocess_pyspark.py pyspark_project\scripts\train_logistic_regression.py api_project\app\main.py api_project\app\repository.py api_project\app\cache.py api_project\app\schemas.py api_project\tests\test_prediction_api.py
```

Preprocessing:

```powershell
& 'C:\Users\nmp10\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' pyspark_project\scripts\preprocess_pyspark.py
```

Training:

```powershell
& 'C:\Users\nmp10\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' pyspark_project\scripts\train_logistic_regression.py
```

API tests:

```powershell
& 'C:\Users\nmp10\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m pytest api_project\tests -q
```

## Validation Results

PySpark preprocessing:

```text
row_count=45211
column_count=19
train_count=36263
test_count=8948
feature_vector_size=59
```

Logistic Regression:

```text
train_count=36263
test_count=8948
accuracy=0.894055
precision=0.704626
recall=0.186265
f1=0.294643
auc=0.779950
prediction_output_count=45211
```

API tests:

```text
4 passed
```

One test warning remains from Starlette's TestClient dependency about `httpx`; app code no longer uses deprecated FastAPI startup events.

## API Verification

- `GET /health` returns status, record count, cache size, cache hits, and cache misses.
- `GET /prediction/client_000001` returns HTTP 200 with prediction fields and metadata.
- `GET /prediction/client_missing` returns HTTP 404 with `USER_NOT_FOUND` and metadata.
- Repeated lookup works and exercises the in-memory cache path.

## Blockers and Notes

- The initial environment did not have `pyspark`, `fastapi`, `uvicorn`, or `httpx`; they were installed into the bundled Python user environment.
- Spark on Windows warns that `winutils.exe`/`HADOOP_HOME` is missing. Training still completed.
- Spark Hadoop writer failed on Windows because of missing `winutils`; the training script falls back to pandas + pyarrow Parquet output.
- Generated prediction output is ignored and should not be committed.

## Milestone Status

| Milestone | Status | Reason |
| --- | --- | --- |
| M6: PySpark preprocessing completed | Done | Preprocessing script reads the real dataset, creates `label`, excludes `duration`, builds stages, and runs successfully. |
| M7: API MVP completed | Done | FastAPI app, `/health`, lookup endpoint, 404 handling, metadata, cache, and tests are working. |
| M8: Basic model trained | Done | Logistic Regression trains end-to-end, metrics are reported, and prediction output is generated. |

## Next Actions for 09/06

1. Add in-memory cache performance comparison evidence.
2. Finalize API docs after any small endpoint polish.
3. Run load test against the lookup API.
4. Consider second Spark model: RandomForestClassifier or GBTClassifier.
5. Keep SparkXGBClassifier deferred unless setup is easy.
